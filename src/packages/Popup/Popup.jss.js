const styles = {
  modal: {
    backgroundColor: 'rgba(0,0,0,0.5)',
    position: 'fixed',
    minHeight: 200,
    minWidth: 200,
    top: 50,
    left: 0,
    zIndex: 5000,
  },
};

export default styles;
